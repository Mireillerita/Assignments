//59.	Write a function that calculates the area of a rectangle.
function calculateRectangleArea(length, width) {
    return length * width;
  }
  
  
  var rectangleLength = 5;
  var rectangleWidth = 8;
  
  console.log( calculateRectangleArea(rectangleLength, rectangleWidth));